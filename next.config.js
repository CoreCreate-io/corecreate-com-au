/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.sanity.io'],
  },
  // Keep any existing webpack configuration you might have
}

module.exports = nextConfig